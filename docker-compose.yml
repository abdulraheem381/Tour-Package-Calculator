<<<<<<< HEAD
services:
  mongodb:
    image: mongo:latest
    container_name: tour-pakage-db
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  backend:
    build: ./backend
    container_name: tour-pakage-backend
    ports:
      - "3000:3000"
    environment:
      - MONGO_URI=mongodb://mongodb:27017/tour-pakage
      - PORT=3000
      - FRONTEND_URL=http://localhost:5173
      - JWT_SECRET=keyboard_cat_secret_change_me
    depends_on:
      - mongodb

  frontend:
    build:
      context: ./frontend
      args:
        - VITE_API_URL=http://localhost:3000/api/v1
    container_name: tour-pakage-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  mongodb_data:
=======


services:
  database:
    build: ./app/database
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: commit2prod_storefront
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - storefront-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p", "root"]
      interval: 30s
      timeout: 10s
      retries: 5
    restart: always

  backend:
    build: ./app/backend
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DB_HOST: database
      DB_USER: root
      DB_PASSWORD: root
      DB_NAME: commit2prod_storefront
    depends_on:
      - database
    restart: always
    networks:
      - storefront-network

  frontend:
    build: ./app/frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - storefront-network

networks:
  storefront-network:
    driver: bridge

volumes:
  mysql-data:
>>>>>>> f0044c4433d58b6d27cf1b70d793ae3f0f49f5ea
